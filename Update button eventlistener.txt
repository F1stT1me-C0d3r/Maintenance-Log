 resultsList.addEventListener('click', (e) => {
        if (e.target.classList.contains('update-button')) {
            const vehicleLi = e.target.closest('li.vehicle-item');
            const formLi = document.createElement('li');
            formLi.className = 'update-form-item';
            formLi.innerHTML = `
            <form id="updateForm">
                <label for="maintenanceMessage">Maintenance details:</label>
                <input type="text" id="maintenanceMessage" name="maintenanceMessage" list="maintenanceOptions" placeholder="e.g. Oil change, Tire rotation, etc." required />
                <datalist id="maintenanceOptions">
                    <option value="Air Filter Change">
                    <option value="Battery Replacement">
                    <option value="Belt Replacement">
                    <option value="Brake Pad Replacement">
                    <option value="Bulb Replacement">
                    <option value="Engine Coolant Flush">
                    <option value="Fuel Filter Change">
                    <option value="Hose Replacement">
                    <option value="Oil & Filter Change">
                    <option value="Tire Alignment">
                    <option value="Tire Repair / Replacement">
                    <option value="Tire Rotation / Balance">
                    <option value="Transmission Fluid Change">
                    <option value="Windshield Wiper Replacement">
                </datalist>

                <input type="number" id="cost" name="cost" placeholder="Cost of maintenance" required />

                <button type="submit">Submit</button>
                <button type="button" class="cancel-btn">Cancel</button>
            </form>`;
            vehicleLi.insertAdjacentElement('afterend', formLi);
        }

        if (e.target.classList.contains('cancel-btn')) {
            e.target.closest('li.update-form-item').remove();
        }